# Verification available now

This repository contains architecture and development documentation and, since M1, a protocol provider and the `cbr` command. The two checks in this section validate documentation structure only; [Product checks](#product-checks) below cover the product.

From this repository's root:

```sh
python3 scripts/check_docs.py
git diff --check
```

Python 3 standard library is sufficient; there is no package install step. The script checks required entrypoints, the local `CLAUDE.md` import, ordinary Markdown file targets, balanced fences and private machine paths in Markdown. It exits nonzero on an error. It reports cross-repository links it could not check.

When all five clones are siblings under one directory, also run from each repository root:

```sh
python3 scripts/check_docs.py --workspace ..
```

This additionally resolves Combraton GitHub main-file links against the sibling checkouts, including benchmarks. It does not prove those checkouts match the remote branches. Record their commits when using the result as integration evidence.

Two GitHub Actions workflows run on pushes and pull requests, both with read-only contents permissions and neither fetching sibling repositories. The **Documentation** job runs `check_docs.py` alone. The **Checks** job runs every command in [Product checks](#product-checks) below, as separate steps, on both `ubuntu-latest` and `macos-latest` — the two platforms Protocol 0.1 supports. No step pipes a command into another process, so a failing command's exit status is never replaced by a successful consumer's. Remote URL reachability, Markdown fragment targets, Mermaid rendering, source-manifest consistency, semantic correctness, live harness instruction loading and product behavior need separate inspection. The script is intentionally small and is not a general Markdown parser.

## Journey verification

[Journey verification](verification/JOURNEYS.md) holds the journey-level acceptance matrix and the per-journey evidence records, aligned with the [shared verification model](https://github.com/Combraton/combraton/blob/main/docs/architecture/VERIFICATION.md). J9, J1 and J8 have been run with no model, J1 again with a model at M4, and J6 has two labelled pilots; the matrix there says which is which. A journey record names its model or provider, or records `none` or `simulated`; a simulated run is never reported as live-model evidence, and `not_evaluated` never counts as a pass.

## Product checks

These exist and run. `rust-toolchain.toml` pins `rustc 1.97.1`, matching the toolchain the pinned Protocol release was built and tested with, so `rustup` selects it automatically. Python 3 standard library is sufficient for the two scripts; there is no package install step.

From the repository root:

```sh
python3 scripts/check_docs.py
python3 scripts/verify_pin.py
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo build --workspace --locked
cargo test --workspace --locked
git diff --check
```

Conformance additionally needs the runner, which is **not** built from this workspace. The vendored runner's manifest inherits `license.workspace` and `edition.workspace` from the Protocol workspace, and the vendored subset carries no `Cargo.lock`, so building it here would need a licence CBR has not selected and could not use `--locked`. It is built from a fresh, checksum-verified extraction of the release archive instead, with the release's own lockfile:

```sh
python3 scripts/build_runner.py
OUT=$(mktemp -d)   # not conformance/results/: see below
python3 scripts/run_fixtures.py --filter stream. --out "$OUT/stream"
python3 scripts/check_results.py "$OUT/stream" conformance/expectations/stream.json
python3 scripts/run_fixtures.py --filter core.   --out "$OUT/core"
python3 scripts/check_results.py "$OUT/core" conformance/expectations/core-c4.json
python3 scripts/run_fixtures.py --filter socket. --participant conformance/participants/cbr-provider-unix.json --out "$OUT/socket"
python3 scripts/check_results.py "$OUT/socket" conformance/expectations/socket.json
python3 scripts/run_fixtures.py --filter evidence. --out "$OUT/evidence"
python3 scripts/check_results.py "$OUT/evidence" conformance/expectations/evidence.json
python3 scripts/run_fixtures.py --filter knowledge. --out "$OUT/knowledge"
python3 scripts/check_results.py "$OUT/knowledge" conformance/expectations/knowledge.json
python3 scripts/run_fixtures.py --filter context. --out "$OUT/context"
python3 scripts/check_results.py "$OUT/context" conformance/expectations/context.json
python3 scripts/run_fixtures.py --filter composition. --participant conformance/participants/cbr-provider-unix.json --out "$OUT/composition"
python3 scripts/check_results.py "$OUT/composition" conformance/expectations/composition.json
```

**Run them somewhere other than `conformance/results/`.** Those directories are **the record of the stage that produced them**, not scratch space: `m1b` holds what M1b measured, when CBR declared `core/1` and nothing else. Re-running into them overwrites that with today's descriptor and today's transcripts, and the result is a committed record that claims to be a stage it is not. The block above used to name them, and following it did exactly that.

**`composition` runs over the socket participant and only over it.** Its fixtures are separate CBR instances reaching one another over public sockets, so the stdio participant has no second role to give them: run with `cbr-provider.json` every one of the fourteen is **skipped**, which reads like a regression and is not one. The last two commands were missing from this block until m4c, which is how that mistake was made.

**The ten `composition` fixtures that need an executor run separately, labelled reference executor, and are not gated** (below):

```sh
python3 scripts/build_runner.py --reference-executor
python3 scripts/run_fixtures.py --filter composition. --participant conformance/participants/cbr-with-reference-executor-unix.json --out "$OUT/composition-reference-executor"
```

**This descriptor runs `--filter composition.` and nothing else.** It claims `execution/1` on the reference provider's behalf, so any other fixture it made applicable would be reported under a CBR-named participant. `run_fixtures.py` refuses it, before anything runs, unless the filter starts with `composition.` and selects no fixture outside that suite, and `scripts/reference_executor_launch.py` starts the reference provider only for a named composition participant configured with `executor` and refuses such a configuration anywhere else ([STATE](work/STATE.md)).

Source identity shells out to `git`, so the tests need `git` on the path; both CI runners have it.

`build_runner.py` downloads the archive once and reuses it afterwards; `--archive PATH` uses a copy you already have and `--offline` refuses to download. It verifies the archive against the pinned SHA-256, verifies every extracted file against the archive's own `BUNDLE-SHA256SUMS`, checks the archive's recorded commit against the pin, and records the runner identity that `run_fixtures.py` stamps into every results manifest.

| Command | What it establishes | What it does not |
|---|---|---|
| `check_docs.py` | Documentation structure: entrypoints, the `CLAUDE.md` import, local link targets, balanced fences, no private machine paths | Nothing about the product |
| `verify_pin.py` | The vendored Protocol material matches the published release, against three anchors CBR does not control: the release's `BUNDLE-SHA256SUMS`, the recomputed inventory `listing_sha256` that also appears in the annotated tag message, and the inventory's own per-file digests | That CBR implements any contract correctly |
| `cargo fmt --all -- --check` | Formatting only | — |
| `cargo clippy … -D warnings` | Lints clean; warnings fail | Correctness |
| `cargo build --workspace --locked` | The workspace builds from the committed `Cargo.lock` with no dependency resolution | Runtime behaviour |
| `cargo test --workspace --locked` | **PLACEHOLDER — updated below from this merge's own whole-workspace run.** Prior figures, kept from both sides pending re-measurement: m5-settle's side said 850 tests over 44 `test result` lines at `23016b1` (848 passed, one ignored, one flake failure not counted), with the round-by-round history down through M1's 92. `origin/main`'s side (peer-capture merge, #49) said 824 tests over 44 `test result` lines at the peer-capture branch (`46036a5`) merged with `main` (`25eefca`, #48): 823 passed, one ignored, none failed, adding fourteen tests in `tests/context.rs` (ten with the fix, four after its second verification round). Both histories describe the same lineage up to `packet_invalid` (#47) and m5-arith; this merge (#49, peer-capture) is measured fresh below. In `cbr-encoding`, every pinned encoding vector — 12 canonical, 18 rejected, 1 command intent — plus the property tests and the strict base64 codec, 20 in all; a rejected vector must be refused **for the reason the vector states**, so a parser that refused everything would fail. In `cbr-identity`, 4 property tests of source identity against real git repositories, each with its negative control (below). In `cbr-provider`, unit tests and integration tests that drive the real binary: production refuses `core-test/1` and every test control; state, events, a grant **and its revocation**, a capability change **and its event**, and **a claim and its decision** survive `SIGKILL` at their positions; **two concurrent socket sessions, one part-way through a subscription, survive `SIGKILL`** with every acknowledged write at its position and the subscriber's last cursor resuming to exactly what it had not seen; a consumer that stops reading standard output ends the process with the bound recorded; **the storage crash matrix** (below), each row killed at its boundary; a second provider over the same data directory refuses to start; **J9**; and **the derived-artifact ancestry control**. In `cbr-cli`, tests that run the `cbr` binary against the real provider over its socket (below). **The only evidence for `core.events.backpressure` and `core.effects` is among these tests** (below). | Any profile conformance on its own |
| `build_runner.py` | The runner was built from the published release archive, with its own lockfile, and its identity is recorded | Anything about CBR |
| `run_fixtures.py --filter core.` | **130 of the 135 `core` fixtures pass, 5 are unsupported by name, none fails** — the most the suite allows a provider that never serves `execution/1`. | The 5, permanently (below). **Nothing about `core.effects` or `core.events.backpressure`**, which no fixture CBR can run exercises. |
| `run_fixtures.py --filter stream.` | Runs the suite and writes the runner's manifest, the transcripts, and a `cbr-run.json` sidecar. The manifest stays byte-for-byte the runner's own output. | Nothing on its own: it reports, it does not gate |
| `run_fixtures.py --filter socket. --participant …unix.json` | **11 of the 13 `socket` fixtures pass, 2 are unsupported by name, none fails.** | The 2, permanently: they declare `execution`. **Nothing about a different operating-system user** (below). |
| `run_fixtures.py --filter evidence.` | **All 16 `evidence` fixtures pass.** | Anything the fixtures do not do: they never kill the provider, never use the socket, and never run `cbr` |
| `run_fixtures.py --filter knowledge.` | **All 10 `knowledge` fixtures pass**, with the `knowledge.store` control. | That CBR's memory is useful, or that any claim is true (KNOWLEDGE §14). No fixture uses `serve_altered_claims`, runs over the socket, or runs `cbr`. |
| `run_fixtures.py --filter composition. --participant …cbr-provider-unix.json` | **3 of the 14 `composition` fixtures pass, 11 are unsupported by name, none fails**: separate CBR instances as the context, evidence and knowledge providers, and the protocol's client-only `minimal-executor`. | The 11: ten need an executor and one needs `verification/1`, and CBR serves neither. |
| `run_fixtures.py --filter composition. --participant …cbr-with-reference-executor-unix.json` | **REFERENCE EXECUTOR, measured at m5g and not gated: 11 pass, 2 fail, 1 unsupported.** CBR is every context, evidence and knowledge participant, and the protocol's reference provider is every executor, chosen per participant by `scripts/reference_executor_launch.py`. **8 of the ten executor fixtures pass**: CBR's context and evidence providers work with a second implementation acting as the executor. The 2 failures are CBR's: its context provider does not submit the investigation execution CTX-18 and CTX-19 expect ([READINESS §7](work/m5/READINESS.md#7-j3-j4-and-j5-and-the-fixtures-that-need-an-execution-peer)). | **Anything about a real adapter or PIO**, and anything about the reference provider. No expectation file: `check_results.py` refuses a `fail`, so the passes are recorded here, not gated. The verification fixture stays unsupported. |
| `result_paths.py conformance/results/*/` | No committed result carries a machine path — the same check `run_fixtures.py` applies when it records a run | That the results are correct |
| `check_results.py` | **The gate.** The outcome multiset matches a recorded expectation exactly: pass count, total, the named unsupported set, and zero `fail`, `timeout`, `harness_error` and `skipped`. | Every suite with no expectation file. All seven have one: `stream`, `core`, `socket`, `evidence`, `knowledge`, `context` and `composition`. `core`'s is `core-c4`, the last of one per stage from `core-c1`. The reference-executor composition run has none. |

### Why the gate is an outcome multiset

The runner's own `coverage_limits` list **stays empty even when fixtures are reported `unsupported`**. Measured against this provider, the `core` suite gives `49 pass, 13 fail, 73 unsupported` with `coverage_limits: []`. So "no coverage limits" is not a gate — a run in which every fixture was skipped for want of a claimed feature would satisfy it. Each suite therefore has a committed expectation under `conformance/expectations/`, naming the exact counts and the exact fixtures expected to be unsupported, and `check_results.py` refuses anything else.

### A permanent coverage limit: five `core` fixtures CBR can never pass

Five of the 135 `core` fixtures declare the `execution` profile and require the `executor.script` test control:

| Fixture | Also needs |
|---|---|
| `core.events.older-consumer-is-closed-without-notice` | `execution.output` |
| `core.events.slow-consumer-is-closed-with-notice-and-resumes` | `execution.output`, `core.events.backpressure` |
| `core.events.slow-consumer-recovery-reports-retention-gap` | `execution.output`, `core.events.backpressure` |
| `core.events.unread-ending-notice-does-not-hold-the-connection` | `execution.output`, `core.events.backpressure` |
| `core.feature-dependencies-match-negotiation` | `execution.context`, and the `evidence`, `context`, `knowledge` and `verification` profiles |

**CBR never serves `execution/1`** — the [consumer handoff](https://github.com/Combraton/protocol/blob/main/docs/work/release-0.1/CONSUMERS.md) states it plainly, and [ADR 001](decisions/001-standalone-v0.1-scope-and-stack.md) keeps it a client only. **No other participant role can satisfy them either:** these are single-participant `core/` fixtures over stdio with `role: provider`, not compositions, so there is no second role for CBR to occupy. `core.feature-dependencies-match-negotiation` would additionally need CBR to serve `verification/1`, which the agreed release scope excludes.

So the maximum attainable on the `core` suite is **130 of 135**, with exactly those five `unsupported`. Any statement of the form "all 135 core fixtures pass" is unattainable and must not be written.

**What is and is not established.** `stream` passes completely; `core` passes every fixture a provider that never serves `execution/1` can pass. `socket` passes every fixture a provider that never serves `execution/1` can pass. `evidence`, `knowledge` and `context` pass completely. `composition` passes every fixture that needs neither `execution/1` nor `verification/1`, which CBR never serves. With the reference provider as the executor, 8 of the ten executor fixtures also pass. That is evidence about CBR's context and evidence providers alongside a second implementation, never about an adapter, and it is not gated. The store **is** durable — SQLite in WAL mode with `synchronous=FULL`, verified by reading the PRAGMAs back from the open connection, and by tests that `SIGKILL` the provider and restart it over the same data directory. There is retrieval — a lexical index, code anchors and a dependency evaluator — and since M3 the deterministic packet compiler reaches it over the protocol ([below](#the-deterministic-packet-compiler-and-what-a-packet-is-compiled-from)); since M4 a model is called, only as [the model runtime](#the-model-runtime) allows. The `context` suite's packets are still the `context.script` test control's, which a production launch refuses.

### Knowledge, source identity and the knowledge verbs

**`knowledge/1` is served in full** (KNOWLEDGE §3–§11):
- claim revisions with the `combraton-knowledge-claim/1` record, whose digest any reader recomputes;
- support classes from declared ancestry;
- authority bindings, transfers and reliance decisions, including the author-is-decider case;
- conflicts and drift;
- the applicability evaluator over `repository_tree`, `dirty_snapshot` and `environment_digest`, with dependencies resolved only as exact local references;
- history with stream positions, rights and events.

**A claim revision is immutable in the database, not only in the code.** Revisions live in an insert-only table whose triggers refuse `UPDATE` and `DELETE`, and each revision commits in the same transaction as the claim subject it numbers. A store test shows both refusals.

33 mutants each fail a knowledge fixture at a named step. Two of them are recorded under CBR's own names rather than claimed as the fixture's narrower mutant.

**Source identity** (`crates/cbr-identity`, PROTOCOL-PIN §5):

| Identity | Definition | Negative control |
|---|---|---|
| `tree` | The git root tree object id, with the commit returned beside it. `cbr ingest --repo` records both as capture anchors. | Two commits with identical content are one tree; a content change is a new tree. |
| `dirty.snapshot_digest` | `sha256` over canonical `{ format, base_tree, entries: [path, status, mode, sha256 or null] }`, sorted by path, with deletions explicit. | A deletion, a mode change and an untracked file each change it. Modification time alone, on a modified or a clean file, staging, and an ignored file do not. |
| `environment` | `sha256` over a declared fact set that **includes build facts**. The record says so in `covers`, because Protocol 0.1 has no `build_digest` condition (G1). | A build fact changes it; the order a file lists facts in does not; a fact declared twice is refused. |

Each row has a mutant that fails its test.

**The derived-artifact ancestry control** (PROTOCOL-PIN §3) runs against the real provider:
- a captured log and two derivations over it are sealed, the derivations under `cbr.derivation.transcript`;
- a claim whose two support entries each declare the log as their complete root is `single_lineage`;
- the producer mistake — each derivation declaring itself as its own root — reads `multiple_lineages`, which is why the control exists;
- a provider mutant that takes each entry's own evidence as its root fails the control.

**J9** is recorded in [JOURNEYS](verification/JOURNEYS.md#j9-an-authority-transfer-invalidates-the-stale-decision-path). It passed with no model, and each of its two named controls has a mutant that fails.

**The `cbr` knowledge verbs** are `propose`, `revise`, `decide`, `evaluate`, `inspect`, `history`, `authority bind`, and the local `basis`. They use the public socket only:
- `revise` reads the current revision and digest through the protocol;
- `decide` reads the binding's epoch and the latest decision the same way;
- `evaluate --repo` computes the target's tree, dirty snapshot and environment digest.

`cbr-provider --issue-credential PRINCIPAL` hands a credential to a second principal. `knowledge_verbs.rs` runs a production provider in which a principal named `model` holds a grant that includes `knowledge.decide` and labels its derivation `human`. It still cannot accept its own claim: `not_authority`, with no record written. The owner's review is recorded with `author_is_decider: false`, and the owner's adoption of its own requirement with `true`. No model runs in this test. `model` is a labelled stand-in principal, not model evidence.

**What M2 does not establish.**
- **That any claim is true, that declared ancestry is honest, or that disjoint lineages are independent** (KNOWLEDGE §14).
- **Untested paths.** `serve_altered_claims` is implemented and has no test. A knowledge subject's visibility in events follows `knowledge.read` without a dedicated test. No fixture runs knowledge over the socket.
- **Source-identity gaps.** Not implemented:
  - the non-git directory tree of PROTOCOL-PIN §5;
  - the `workspace` clean/dirty determination under a lock. A snapshot is read without one, so a working tree changing during the read gives a snapshot of no single instant;
  - submodule contents, which are recorded as a gitlink with no digest.

  Identity is computed with `gix` in this process, since m3b; it needs no installed `git`.
- **Cost of reads.** `inspect` and `history` scan every record of a kind, which is proportionate to v0.1 counts and not measured beyond them.
- **CLI coverage.** `cbr` has no verb for conflicts, transfers or grants.

### Context: requests, packets and claims in packets

**`context/1` is served with all seven features** (CONTEXT §1–§14) — `context.advisory`, `context.required_before_start`, `context.required_before_transition`, `context.shared_jobs`, `context.updates`, `context.expand` and `context.claims` — over stdio and the Unix socket.
- **Requests.** Step 2 refuses an item that cannot be checked, a misplaced transition and a commit-only basis declared complete. Step 3 refuses an obligation, or a claim check, whose feature the session did not negotiate.
- **Results.** An item is satisfied only by content that meets its check. A required item missing at the deadline is `unmet`; an advisory one follows its fallback. The investigation budget, the output capacity and the deadline are three separate reasons, never reported as one another.
- **Capacity.** Output capacity is reserved for required items' sections before any advisory content, and mandatory content that cannot fit refuses the request with `budget_insufficient` and the size it needs.
- **Packets.** Each revision is an exact sealed evidence artifact of canonical `combraton-context-packet/1` bytes, or `/2` under `context.claims`. It is sealed in CBR's own store with CBR as producer principal, or at a separate evidence provider over that provider's public socket, as CBR's own principal under that provider's grant. A revision is never reported before its artifact is sealed. Updates are new revisions; an old revision keeps its artifact and is never current again.
- **Claims.** A claim is carried as an exact snapshot after its record digest is recomputed. A label never promotes an unaccepted claim, and only `invalid_for_target` makes a section stale. Reading a packet under `context.claims` re-reads each carried claim, locally or at a knowledge provider, and reports `claim_changes`, claim invalidations and unverified items. The published facts never change.
- **Reads.** `context.packet.inspect` serves an exact excerpt that never carries a digest. `context.expand` serves a cited artifact the reader may read, and refuses an unreadable, a nonexistent and a foreign citation identically.

**Where packet content comes from.** A scripted packet is prepared by the `context.script` test control, which is where every conformance fixture's packets come from. The script says which sections, coverage, omissions, corrections and unmet reasons a job produces. CBR decides everything the protocol makes the provider responsible for: satisfaction, inclusion, labels, claim snapshots, sealing and the read-time facts. The provenance names the compiler `cbr-context-script`. A production launch refuses the control, and since m3c a production request is compiled instead ([below](#the-deterministic-packet-compiler-and-what-a-packet-is-compiled-from)). A request whose deadline passes before anything prepares it is published with every required item `unmet`, which `a_request_nothing_prepares_is_published_unmet_at_its_deadline` shows.

**The composition fixtures run separate CBR instances**, each with its own store, reaching one another only over public sockets under grants:
- a context provider sealing at a separate evidence provider, including while that provider is killed and restarted;
- one grant per audience for direct fetch;
- the protocol's independently written, client-only `minimal-executor`, vendored from the release archive, enforcing its dispatch boundary from CBR's read-time claim facts, with CBR reading claims at a separate knowledge provider.

The other 11 composition fixtures declare `execution/1` or `verification/1`, and are out of reach **with CBR in every role**. Since m5g, ten of them run with the protocol's reference provider as the executor, labelled **reference executor**: 8 pass, and 2 fail on CBR's missing context-initiated investigation (CTX-18, CTX-19). The one that needs `verification/1` stays out of reach, because its single participant would have to serve it ([READINESS §7](work/m5/READINESS.md#7-j3-j4-and-j5-and-the-fixtures-that-need-an-execution-peer)).

**CBR's own tests** (`crates/cbr-provider/tests/context.rs`, and unit tests in `context.rs` and `store.rs`) cover what no fixture reaches:
- a publication whose seal is held at the evidence provider past the peer timeout replays after the clock moves, because the first attempt's capture instant is kept, and the diagnostic it logs does not contain the peer credential;
- a packet sealed at an evidence peer whose tick's batch is then lost, to a kill or to a commit that fails on SQLite's write lock, is published once the clock has moved, under the peer's digest and first `captured_at`, because its capture instant is kept in a commit of its own before the send; so are both of two packets one tick sealed, and a later revision of a request already published; a tick that cannot keep its instants sends nothing; a revision retried after the deadline, whether its first attempt was killed or its seal timed out, is published as composed at its kept instant, while a revision first composed after the deadline reads `deadline_passed` (the crash-matrix row below);
- a packet killed after its object is published and before its batch commits is published exactly once after restart, and at the kill its object is on disk while its request is still `preparing` and no artifact row names it (the crash-matrix row below);
- a correction after publication is reported at the read beside `superseded_by`, and content derived from a corrected authority revision is stale whether prepared before or after the correction;
- capacity is reserved for required content whatever order sections were prepared in, which the reference provider does not do;
- a provider-origin batch commits whole or not at all;
- every packet a tick publishes leaves it, whichever step published it: a packet published at its job's ending is served by its digest, and so is one the deadline alone publishes, and so is each of two published in one tick. **That its object is written before the tick's batch commits is tested only for a `publish` step** (the crash-matrix row and the replay test below); the job's-ending and deadline-alone tests show the revision served after the tick, not that ordering. And a tick whose second send to an evidence peer fails keeps both packets' capture instants, so both are published, sealed at the peer with the first attempt's instant, once the peer is back and the clock has moved;
- a packet the publication guard refuses ends its job as `packet_invalid`: the request is `refused`, and so is every other subscriber of a shared job still preparing, one with a later deadline among them, one listed after a subscriber already published at its own deadline, and one whose own packet would have been valid, each refusal naming the job and each recorded before the job's ending; the job is found by its own id when its first subscriber was cancelled; one published `ready`, `unmet` or `partial` under `context.updates` keeps its state and current revision; an ended job takes no new subscriber, and keeps the ones it had, refused or published, so a reader of any of them still sees it end; each item ends by its obligation, read in the order the request named them; the refusal is recorded at the provider clock's instant; a store failure while the job is being ended fails the tick and logs no refusal, and the next tick ends the job once; the log line, written once, says the job has ended and names no id; no packet of the refused tick, a valid one included, is sealed in the store or sent to an evidence peer, whichever step of the tick published it — a `publish` step, a second one in the same tick, the job's ending or the deadline; and a subscriber cancelled before the ending reads its items after it as before ([the compiler section](#the-deterministic-packet-compiler-and-what-a-packet-is-compiled-from));
- that ending survives a `SIGKILL` before its batch commits, when nothing was reported or logged and the restart ends the job once (the crash-matrix row below), and after, when the restart serves the same inspect byte for byte and compiles nothing again.

**One thing is held the other way round, by a fixture and by no cargo test**: the `source` a peer packet's descriptor names, the request the packet was published for (`Outgoing::Peer`'s `request` in `context_ops.rs`). `composition.packets-are-sealed-at-a-separate-evidence-provider` checks it at step 9. `packet_invalid`'s fifth verification's mutant K4, which names the packet's artifact id there instead, passed every `cbr-provider` and `cbr-cli` test and failed only that fixture: *result/descriptor/source/id: expected "r-1", found "packet.r-1.1"* ([STATE](work/STATE.md)).

Of 57 mutants, 48 fail a context or composition fixture at a named step and 9 fail only CBR's own tests; [STATE](work/STATE.md) lists every one.

**Packet bytes are kept by the context record too, and that is a retention limit.** A revision's published facts keep its body, which `context.packet.inspect` excerpts after re-encoding it canonically; it is served only while it still digests to the reference. So a packet sealed at a separate evidence provider has a copy at the context provider, and purging a packet artifact ends `evidence.fetch` of it but not its excerpt. Request retention is not implemented.

**What m3a does not establish.**
- **That CBR prepares useful context.** Scripted content is test environment (CONTEXT §12). Nothing is retrieved or compiled.
- **Investigation executions.** CBR serves no executor role, and its context provider lacks the `execution/1` client role CONTEXT §4 gives it: submitting an investigation to a configured executor as its own caller (CTX-18, CTX-19). A script step it does not perform, `execute`, holds its job rather than pretending it ran. m5g found this is exactly what fails two of the ten executor fixtures run with the reference executor ([READINESS §7](work/m5/READINESS.md#7-j3-j4-and-j5-and-the-fixtures-that-need-an-execution-peer)).
- **Responsiveness under a stalled peer.** A context provider calls peers while holding its processing lock, bounded by a three-second timeout per frame, so a stalled peer delays every request to that provider for up to that long. Preparation runs on every request and idle poll of an authenticated session, and its cost grows with the number of running jobs.
- **Untested paths.** Skipping preparation for an unauthenticated connection has no test. A `packet.<request>.<n>` id that collides with an existing artifact stops that publication with a diagnostic and has no test. *(Until `packet_invalid`'s fifth verification this bullet also listed event visibility of `context.job` subjects under a grant, which follows the requests' `context.read`, as having no dedicated test. It has two since `packet_invalid`: `an_ended_job_is_still_seen_through_a_subscriber_it_is_not_named_after` and `a_published_subscriber_not_named_by_its_job_still_sees_the_ending`, T18 and T21 in [STATE](work/STATE.md), in each of which a reader whose grant covers only a subscriber the job is not named after reads the job's `context.job.ended` through `core.events.read`.)*
- **Read cost.** A request record holds every published revision's facts, and each tick parses every job.
- ~~A tick whose batch fails to commit after an evidence peer has sealed its packet leaves the request `preparing` for ever once the clock moves.~~ **Fixed by the peer-capture change**: a packet's capture instant is kept, in a commit of its own, before it is sent, and a kept revision's deadline is judged at that instant, so the retry sends the same bytes and the peer replays them ([STATE](work/STATE.md), *This change — a peer's seal survives a lost commit*). **Still not established:** a retry whose bytes change for a reason other than the deadline, such as a model-assisted compile after a kill, which would still meet `idempotency_conflict`, its digest changed; a peer that discards its records of the first attempt between attempts, since CBR sends each attempt under the generation it last observed rather than one it persisted (CORE §6.3 item 3); a send that timed out, was sealed by the peer later, and was followed by a refusal, which leaves that seal beside the refused request, the peer's state CBR cannot roll back; and the clock-driven scripted case, pre-existing: a retry after the clock has passed a `wait_until` the killed tick had stopped at recomposes the revision from more sections than the peer's seal, so the peer answers `idempotency_conflict` at every later tick too. **This path is conformance-only:** `evidence_provider` is a member of the `context` launch member, a test control a production configuration refuses.

### Retrieval, code anchors and the dependency evaluator

These are **derived** memory: computed from the journal, replaceable without changing what the journal says, and never authority. Each records enough to be checked rather than trusted — the tree an index was built from, the tree and blob an anchor was taken at, and the exact inputs an evaluation read. They live in `crates/cbr-memory`, work on a caller-supplied connection, and are reached over the protocol only through the packet compiler, since m3c.

**The dependency evaluator** (STACK §7) is salsa's algorithm, written small against SQLite so it runs inside the caller's transaction. Per memo it keeps `changed_at` and `verified_at`, ordered edges with a reverse index, early cutoff by backdating, and durability levels. Its two guards are that backdating is refused when a recomputation's durability decreased, and that a memo which read something untracked is never validated without re-executing.

**Its acceptance is a property harness, not an example**, as STACK §7 requires: 200 seeded random programs with dependencies that change with the values read, functions that frequently recompute to the value they had, inputs whose durability changes, untracked reads, and a database reopened mid-run. Demand-driven evaluation must agree with a from-scratch recompute every time. The harness **found a soundness bug in the first seed**: an input whose durability decreased announced its change only at the new, lower level, so a memo that had recorded the higher durability still shallow-verified against it and served a stale value. A write now announces at the higher of the old and new durability. That is the failure a hand-rolled evaluator is most likely to have and least likely to notice, and it was caught before anything depended on it.

**The lexical index** (STACK §5) is SQLite FTS5 with text normalised in Rust first, because FTS5 has no camelCase splitting and stemming is harmful on identifiers. Each run of letters and digits is stored whole and as its parts, and a query asks for the parts: `user name`, `name` and `getUserName` all find the identifier, `getUserName` also finds prose that says "get user name", and `flush` still does not find `flushed`. The engine was chosen for atomicity rather than features, and that argument is tested — a rolled-back transaction leaves nothing searchable.

**Code anchors** (STACK §6) come from each grammar's own `tags.scm`, for the languages CBR pins and only those: Rust, Python, JavaScript, TypeScript and TSX. A file in any other language is indexed as text and has no anchors at all, which is a visible gap rather than a guess. Tags find **where a name is defined**, never which definition a call refers to, so **every candidate is kept and ambiguity is marked** (INTERNALS §2). Each anchor records its tree and blob, so one that has gone stale is detectable.

**Indexing a tree** reports its own coverage: blobs seen, indexed and anchored, and the three gaps — binary, too large, and an unanchored language. **A dirty working tree adds three more**, one line each and each broken down by kind: files that are untracked and in no tree, tracked files **modified** on disk, and tracked files deleted from it. The index is built at the committed tree, which is right — a working tree is not a basis anyone else can resolve — but M3d's Knowscroll pilot searched twenty-one modified files at their committed bytes with nothing saying so, which is a false absence by the same rule that made the untracked gap exist. An empty search result is not proof of absence (INTERNALS §5), so a caller must be able to tell "nothing matched" from "never looked". Indexing is per tree and replaces that tree's rows, so re-indexing is idempotent and each tree answers only for itself.

**The index's compiler version is guarded by a digest, not by memory.** `retrieval::COMPILER` is what tells a caller an index is this build's index, and in M3d it was found to have gone stale: the chunker had changed and the string had not, so an index from the previous build would have passed as `complete`. The guard added in its place is `retrieval::GOLDEN_FIXTURE_DIGEST`, over a fixture tree written byte for byte in `crates/cbr-memory/tests/golden_index.rs` — its chunk rows, its anchor rows, its coverage report, a fixed set of query results, **and `COMPILER` itself**. A build change with no version bump fails the test; a version bump with no build change fails it too. Ranking order is outside the digest on purpose: BM25 is unevaluated, and a guard that fails on a scoring tie gets disabled rather than fixed. The packet compiler has the same guard in the same shape, `GOLDEN_PACKET_DIGEST`, over the packet a fixed fixture produces; it binds `compiler::COMPILER` because a sealed packet's coverage names its producer.

**Source identity moved to `gix`** here, in this process: no subprocess, no shell and no runtime dependency on an installed `git`. Reading a tree's blobs in bulk is what made the difference, and the four identity property tests with their negative controls pass unchanged, which is the acceptance that move was promised on. (ADR 001 question 11 names the trigger and now records that it fired here, with those four tests as the acceptance it met.)

**What m3b does not establish.**
- **No packet.** Nothing here compiles context; that is m3c.
- **Ranking is BM25 and nothing else.** Whether what it returns is what a task needed is unevaluated. INTERNALS §7 says retrieval scores alone cannot establish product value, and none is claimed.
- **Anchors resolve names, not references.** Generics, macros, re-exports and shadowing stay ambiguous by design.
- **Untested at scale.** The largest indexed tree is a test repository. Index size, the 20-line chunk and the 1 MiB blob cap are unmeasured choices.
- **The evaluator has no caller yet**, so its durability levels and untracked marks are exercised only by its harness.
- **The rules that govern a retrieval answer are not built.** An index carries no build manifest — no source frontier, no compiler version, no `complete` / `lagging` / `unavailable` state — so a reader cannot tell a current index from a stalled one. Nothing yet forbids a lagging index from answering "nothing found" instead of a bounded canonical fallback or an explicit incomplete coverage, which INTERNALS §5 requires and which journey 1's negative control depends on. There is no test that an index rebuilt from the canonical records equals the incrementally maintained one. **Search has no principal**: nothing filters a result by what the caller may read. And bounding is one `LIMIT` on a row count, with no pagination cursor, no span or byte cap per read and no aggregate cap across a batch. These five are m3c's first commits, each with its test, before any packet is compiled; [STATE](work/STATE.md) records them with their reasons.
- **Non-git trees still have no identity** (PROTOCOL-PIN §5), and a submodule is still a gitlink with no contents.

### The deterministic packet compiler, and what a packet is compiled from

A production request with no script is **compiled**, not left to its deadline. The compiler is `cbr-context-compiler/1`, a provenance deliberately distinct from `cbr-context-script`, so a scripted packet and a compiled one are distinguishable in any record without reading their content.

**The rules a retrieval answer owes its caller** (INTERNALS sections 2 and 5) come first, because the compiler is their first consumer and journey 1's negative control is meaningless without them:
- **A build manifest** names the source frontier an index was built to, the compiler that built it and the coverage it reached, so a projection is `complete`, `lagging` or `unavailable` against the basis a caller actually asked about.
- **Nothing found is never the answer.** A projection that is not complete either falls back to the canonical records — the repository's own objects, read and chunked exactly as the index would have, bounded by a read budget — or states why it could not. A lagging index is not consulted at all: its rows describe a different tree, so a hit in it would cite the wrong bytes.
- **A search answers inside a view.** A repository the principal may not read is absent from the view, so it is never searched and never named; "not allowed" and "no match" are the same answer, which a test asserts by comparing them.
- **An answer is bounded three ways**: rows per page with a keyset cursor, bytes per read, bytes per batch. The keyset is applied in SQL rather than to an already-truncated page, which is a bug the paging test caught before it was committed.
- **A rebuilt index equals the maintained one**, which is what makes an index derived rather than authority.

**Registration is provider configuration, not a protocol operation.** The protocol defines no repository subject and no operation that would create one, and the one fact registration needs — a checkout path — names the operator's own filesystem. So the path is given at launch with `--register-repository <id>=<path>`; what the protocol sees is a `cbr.repository` subject holding the id and the instant, and a grant over that kind with the `context.read` right is what makes a repository readable. A registration whose checkout cannot be read **refuses the launch**, because a registered repository that answers nothing is exactly the false absence everything above exists to prevent.

**The six steps of INTERNALS section 5** are: the view and the basis resolved at the command, where the grant is, and handed to preparation so it cannot widen them (1); a frontier chosen by building or reusing an index (2); retrieval inside the view (3); applicability through the claim and condition machinery Knowledge already owns (4); the budget allocated mandatory-first, with a compiled request whose mandatory content cannot fit ending `budget_insufficient` (5); and the packet sealed by the same code a scripted packet is sealed by (6). Compiling produces a *script*, spliced in place of its own marker, so everything downstream is the path m3a's fixtures already measure.

**A citation resolves to an exact span at a named tree.** Each cited file is sealed as an evidence artifact under its blob id, with the tree, the repository and the path as capture anchors. Journey 1 fetches every citation and compares it with `git cat-file blob`, and checks that each section's excerpt is the artifact's own bytes at the line its locator names.

**A packet's ids are protocol identifiers, whatever the repository holds** (`cbr-context-compiler/4`). `core/1`'s `identifier` is `^[A-Za-z0-9][A-Za-z0-9._:~-]{0,127}$`, and the `context` schemas require it of every section id, citation id, omission and artifact id. Until `/4` a discovered span's ids carried its path — `dc-span-crates/cbr-cli/tests/journey_two.rs-67989` — so a compiled packet held schema-invalid ids and the provider's own `context.expand` refused them: only a citation of a file at a repository's root could be followed. No fixture caught it because conformance fixtures script packets rather than compile them. `crates/cbr-provider/src/ids.rs` now builds every id a packet names. A path or an anchor name is **encoded**: `[A-Za-z0-9._-]` as itself, `/` as `:`, every other byte as `~XX` in uppercase hex, which is injective and reads back with one rule; a span is `span-<repository>:<path>-<start byte>` and an anchor `anchor-<repository>:<name>`, so the same path at the same byte in two repositories of one basis is two sections. An id that is an identifier as it stands is **unchanged** — `s-log.o1`, `c-log`, `d-claim-…` — and one that would pass 128 bytes keeps its prefix, then `~~`, then 32 hex digits of a SHA-256 of the rest, then the longest tail that fits, starting at a `:` where one does; `~~` never comes out of the encoding and an unshortened id never has `~` after its prefix, so the two forms cannot meet. A section's locator still names the path in full, so a reader never decodes an id. Ties in the drop order are broken on a **key** holding the id as `/3` spelled it, so nothing moved: the golden packet differs from `/3`'s only in its sixteen span ids and the compiler string, and writing those back reproduces `/3`'s digest exactly ([STATE](work/STATE.md)). **A last check before publication**: `context::ids_outside_grammar` walks every path the vendored `context.packet.inspect` result types as an identifier — a unit test walks the schema, through its references, and fails on a path neither checked nor named with its reason — plus the sealed body's sections and citations, the packet artifact id, and that no section id or citation id repeats; an id the schema requires of an object that is there is refused when it is absent or `null` as well, and the list of the ones it does not require is checked against the schema's `required`. It runs in `publish_one` on every packet a tick publishes, and no packet of the tick is captured, sent to an evidence peer or sealed until the tick has run to the end with every one of them guarded (`seal_packets`, since `packet_invalid`'s fourth fix round); a packet that fails is not published, and nothing of that job's tick is committed or sealed, a valid packet of it included. **The job then ends, as `packet_invalid`** (the owner's ruling of 2026-09-26, [m5 READINESS](work/m5/READINESS.md) item 7), in one provider batch of its own, built from the job and its requests as stored and never from the refused tick: every subscriber still `preparing` is `refused` with reason `packet_invalid`, with no `needed` and each item `unmet` if required and `degraded` if advisory for that reason, never `satisfied` or `pending`; a subscriber already published under `context.updates` keeps its state and its current revision; and the batch records `context.request.changed {state: refused, job, reason}` for each request it refuses, then `context.job.ended {reason: packet_invalid}`. The provider logs the job, the request and the JSON pointers but never the ids, once, after that batch commits, and the tick moves on to the next job; an ended job is never compiled again. **Why the shape conforms.** CONTEXT's reasons are open: an item's may be "a provider reason" (§3), ending a job is "reported as `context.job.ended` with its reason" (§4), and the inspect and submit result schemas type `reason` as a string of 1 to 64 characters with no enum. The request state is a closed enum, so no state is added. `ready`, `partial` and `unmet` each name a published revision — "When a request reaches `ready`, `partial` or `unmet`, the provider publishes a packet revision" (§5) — and there is none to name; `cancelled` is the caller's; `refused` is the one terminal state the provider can choose that needs no packet. **That the request ends `refused` is this session's reading within the owner's ruling**, which named the job's ending and its reason and not the request's state. It reads §3's "After preparation the request is `ready` … `partial` … `unmet`" as not reached: preparation never produced a revision that could be published, and §5 and §12 ("Until the artifact is sealed, the revision is not published and the request stays `preparing`") tie those three states to one. What establishes it: J1 checks every id and expands every citation; `crates/cbr-cli/tests/expand.rs` compiles over a repository with a 300-byte path, spaces, letters outside ASCII written in both normal forms, `..`, `~`, `:`, `%`, a `.hidden/` directory and a Python function named `größe_wert`, and over two repositories holding the same path, and follows every citation to its `git cat-file` bytes; two tests in the same file following an item id of 127 characters, as a source item and as an evidence item whose projection declares its bytes omitted, to a published packet with shortened ids and a citation that expands to the bytes; a seeded property test in `ids/tests.rs` over paths to 300 bytes, repository ids of 1 and 128 characters and item and claim ids either side of each prefix's limit, which also checks each shortened id's 128-bit digest, with its width computed apart from the module's constant, and, for span and anchor ids, that the tail is the longest that fits (for the other kinds a pinned test fixes the tail); the guard's unit tests; and `crates/cbr-provider/tests/context.rs`, where a scripted section `s/…`, a section with no `section_id` and a citation with no `citation_id` are not published while a valid job beside them is, each such request ends `refused` as `packet_invalid`, and a 127-character request is sealed under an artifact id inside the grammar. **The ending** is held by thirty-two more tests in that file, T1–T32 in [STATE](work/STATE.md) (twenty-seven until the fifth verification, which found the last five uncounted) — the request's inspect, its events and the job's, in order and exactly, and the stored job as it was before the refused tick; ten more ticks and a `SIGKILL` and restart, with one log line in all; every subscriber of a shared job; an update that leaves its published revision current; a tick that published a revision and then refused the next, which commits and seals neither; an item satisfied while preparing that ends `unmet`; a kill before the ending commits (the crash-matrix row below); a refusal at the deadline; another job published in the same tick as the refusal, walked after it; and six written after verification, for what its surviving mutants and its claims check reached: a shared job whose first subscriber was cancelled, ended for the one left; every refusal in a shared job naming the job, not the request; a subscriber whose deadline is later than the one that brought the packet on, refused with it; a later request that does not join the ended job; revisions published `unmet` and `partial` kept when the next update is refused; the log line, pinned whole; and three written after a second verification, for what its surviving mutants reached: a refused request's items, one of each obligation, each ended by its obligation and read in the order submitted; the refusal and the job's ending recorded at the provider clock's instant when the guard refused, not the submission's; and the ended job still seen, through `core.events.read`, by a reader whose grant covers only the subscriber it is not named after; and four written after a third verification, for what its surviving mutants and its probes reached: a narrower subscriber whose own packet would have been valid, and is published `ready` alone, refused with the job all the same, by this session's choice, which the owner may overturn ([STATE](work/STATE.md)), its packet never published, served or sealed; a store failure while the job is being ended, made by holding SQLite's write lock from a second connection, failing the tick with no refusal logged, and the next tick ending the job once; a subscriber published under `context.updates` and not named by the job still seeing it end; and every subscriber's refusal recorded before the job's ending; and five in a fourth fix round, on a defect the third verification's probe showed: that narrower subscriber's valid packet never sent to an evidence peer, nor a tick's valid first revision when the tick refuses its second; the job's ending and the deadline, each refusing the wider subscriber, leaving no object, and, since the fourth verification, each run a second time beside an evidence peer, which never hears of either packet; and a subscriber cancelled before the ending reading its items after it as before; and five after a fourth verification, for what its surviving mutants and its claims check reached: a packet published at its job's ending, one the deadline alone publishes, and each of two published in one tick, each served by its digest; a tick whose second send to an evidence peer fails keeping both packets' capture instants, so that both are published, sealed at the peer at the first attempt's instant, once the peer is back and the clock has moved; and a subscriber published at its own deadline not stopping the ending for one still preparing that its job lists after it — by `context::tests::a_request_refused_after_preparing_satisfies_no_item`, and, through `cbr`, by `a_request_whose_packet_would_carry_an_id_outside_the_grammar_ends_refused_and_says_why_through_cbr` in `expand.rs`, which also shows `cbr packet`, `cbr expand` and `cbr cancel` failing and the same bytes after a restart. **What it does not establish**: a compiled job's refused tick leaves the objects of the sources and derivations it sealed while compiling, uncommitted, once, for the start-time collection pass — they are not packets; a valid revision a script publishes in the same tick as a refused one, and another subscriber's valid packet in it, are never sealed by that tick and never delivered, which is this session's choice to refuse the whole job ([STATE](work/STATE.md)), though a revision whose send to an evidence peer timed out in an earlier tick may since have been sealed there, and stays sealed beside the refused request, since the guard cannot undo a peer's seal (STATE, choice (d) and follow-up 8, shown by the fifth verification's probe P1); the refused shape is checked by assertion rather than by a schema validator, and no fixture reaches the guard; and item `x`'s omission `s-x.o1` is still the section id of an item named `x.o1`, a recorded follow-up.

**A measured limit of ranking, recorded rather than fixed.** In J1's run at m3c, BM25 placed **three spans above the span that answered the question**: two from this repository's own test file, which quotes the question, and one from an unrelated module that shares the words *repository*, *checkout* and *basis*. The answer was found and cited, and it was fourth. That is what a lexical index does with a prose question, and no amount of re-weighting inside BM25 addresses it. M3d **measures** it — each pilot's packet is scored against an oracle the owner wrote, and against a plain `git grep` baseline over the same tree — and M4 is where retrieval gets something other than term overlap to work with. Nothing here is tuned against a pilot question.

**A chunk is bounded in bytes as well as lines.** Twenty lines of a Markdown table is nearly nine kilobytes: one chunk held a whole decision table, so ranking could not tell one decision from another, and no bounded excerpt of it could show the row that answered a question. `lexical::CHUNK_BYTES` matches `compiler::EXCERPT_BYTES`, so a chunk is a thing a packet can show in full. Three windowing heuristics were tried before this and all three were fitted to one example; the defect was the retrieval unit, not the window.

**A section carries bytes, not a pointer.** A locator line says where the span is and how it was found; the excerpt under it is a **contiguous byte range of the cited artifact**, at most 2,048 bytes, and the locator states which range it is so a reader checks it rather than guessing. That is what the output capacity counts, so the budget measures what a consumer reads rather than the size of a list of file names.

**A ranked span reaches its neighbours.** A chunk boundary is an artefact of indexing, and in both M3d pilots the sentence that answered the question sat a few lines past a cited span's edge on the far side of a fixed twenty-line boundary. A span that **ranked** is extended into the chunk either side of it while the excerpt stays within `compiler::EXCERPT_BYTES`; chunks tile a file exactly, so the result is still one contiguous byte range of the artifact, which is what the locator promises. When only one side fits, the side holding more of the query's terms wins, and on a tie the following chunk does. A file cited because nothing matched in it is not extended, having ranked nothing. **Overlap is decided after widening and against every span the packet already holds** — the first form of this checked a hit's own start byte against the items' selections only, and two adjacent hits that widened into each other were published as the same section twice.

**A symbolic link never supplies an item's content.** A link's blob holds the target's *name*, so citing it hands a reader a path where they asked for a file: Knowscroll's `AGENTS.md` is mode `120000` and a packet reported the item satisfied by nine bytes reading `CLAUDE.md`. A link is resolved inside the same tree, relative to its own directory, refusing an absolute target, one that climbs out, and a chain over eight hops; landing on a regular file of the same tree satisfies the item **from the target, cited at the target's path**, with the locator naming the link. Anything else leaves the item **unmet** with reason `source_is_a_link`, and the coverage carries the link and where it pointed, because CONTEXT bounds an item reason at 64 characters. A `source_included` check is satisfied by the section's path **or** by the link recorded beside it, both sealed in the packet, so satisfaction stays decidable from the packet alone (CONTEXT §3).

**Retrieval is scoped to the named path, in SQL.** An unscoped search ranks a named file's best span against every other file's, and in a real repository the file's own span loses: the first version of this took rows from the whole repository and filtered by path afterwards, and cited a file's first twenty lines while the answer was on line 24. A **multi-term selector has a recorded ladder** — every term inside one chunk, else the best ranked partial match, else the file's opening chunk, marked `first-chunk` so a reader knows no term was found in it at all.

**A question is prose, and a selector is chosen words.** Asking for every term of a task sentence returns only chunks that quote the sentence, which in a repository containing the test that asks it is exactly what happened. Discovery therefore uses the ranked partial reading, where BM25 weights the rare terms that carry the question; `select_source`, given a path and a selector, keeps the every-term reading.

**Discovery reads only what the submitting grant could read, and authorization happens at the command.** A claim is read through `knowledge_inspect`, which is the body of an operation whose step 6 ran before it — so calling it during preparation, on the provider's own authority, authorizes nothing. The first version of discovery did exactly that and put every claim in the store into every packet, including for a principal holding no knowledge right at all. The job now carries the claims the submitting grant could read, resolved where the grant was, `may_join` compares that set, and preparation reads nothing outside it. A claim outside it is never named: a test asserts the packet is **byte-identical** to one prepared in a store where the claim was never proposed, and the same test exists for a repository outside the view. **How this shipped:** the view test covered repositories, and nothing covered claims.

**A claim cannot cite a span of an artifact.** Protocol 0.1's evidence reference is a provider, an artifact and a digest, with nothing narrower ([`check_evidence_reference`](../crates/cbr-provider/src/knowledge.rs)). A repository whose decisions live in one append-only file therefore gets one artifact and many claims citing the same bytes, with the line range that distinguishes them carried in each claim's scope rather than in its evidence reference — where a reader would look and where a verifier could check. Found while ingesting Knowscroll-v2's twenty-two decisions at M3d, and measured in the packet that run produced: twenty-two claim sections citing the same artifact and the same digest, twenty-two times ([JOURNEYS](verification/JOURNEYS.md#j6-pilot-knowscroll-v2-decision-memory-no-model--failed-then-passed-on-the-rerun)). Reported to the protocol as [Combraton/protocol#16](https://github.com/Combraton/protocol/issues/16), which describes the case and proposes nothing normative. It belongs with gap G1.

**A discovered claim is a claim section.** It carries the same `claim` reference an asked-for one carries, so `context::claim_snapshot` and CONTEXT §14's read-time facts see it: reject a carried claim after publication and the read reports the change without the sealed bytes moving. It cites the evidence its claim rests on, because a binding statement a reader cannot check against a revision and a digest is an assertion rather than a citation. The label vocabulary has no `rejected`, so a rejected claim is `stale` — which alone would say "was once valid" — and its content says it was rejected and names the decision that did it.

**Relevance is a stated rule, not a vibe — and eligibility is not selection.** A claim is **eligible** when one of its conditions names a repository the basis names, or when its scope or statement shares a term with the question; anything else is omitted with reason `applicability`, counted. Eligibility is decided from the claim alone, because reading cited evidence to decide it would make every claim over a large shared file eligible for every question.

**That test selects nothing in a one-repository store**, where its first limb is true of everything: M3d's Knowscroll packet carried **all twenty-two** of that repository's decisions, every one labelled `binding`, for a question about one of them. Among eligible claims the compiler therefore **ranks** — the question's terms against the statement, the scope's qualifiers, and the text of the cited evidence **at the span the claim names** — carries `compiler::CARRIED_CLAIMS` of them, and omits the rest with reason `applicability` and a count. A claim that does not rank is omitted, not demoted: a `binding` claim that ranks keeps its `BindingClaim` drop rank. The span is read from the scope's `lines` qualifier, which is where a claim over a shared artifact must put it while [protocol#16](https://github.com/Combraton/protocol/issues/16) stands; without it every claim over one file scores identically and the ranking is no ranking. Measured on the same repository at M3e: **four carried, eighteen omitted and counted.**

**What "drop order" decides, exactly.** `context::inclusion` packs rather than truncates: it walks sections in order and keeps each one that still fits, so a small section low in the order can occupy leftover room a larger one could not use. The order therefore sets **priority, not exclusion**, and what it guarantees is which section loses when two compete for the same bytes.

**Drop order follows INTERNALS §5 step 5** — mandatory first, then task evidence, then optional context — rather than the section id, which dropped by path name and let an anchor outlive a binding decision. Ranked: binding claims, other current claims, spans in retrieval's own order, anchors, then historical material. A test runs one request at a capacity where about half the advisory content fits and pins that the binding decision and the code the question is about outlive the rest.

**Beyond what the items name, the compiler discovers** (CONTEXT §6): spans the question finds anywhere in the view, at most two per file so one file cannot take the whole budget; anchors for the **identifier-shaped** names in the request — not every word of the task, because ordinary words are function names somewhere in any large repository; and every claim this provider holds, judged against the request's basis. Claims are labelled by what the authority permitted them for, so `binding` means an authority said so; a rejected claim, or one not applicable at this basis, is carried as `stale` and historical, which is INTERNALS §5 step 3's requirement that rejected alternatives be distinguishable rather than absent. All of it is advisory, so `context::inclusion` reserves nothing for it and drops it with reason `output_capacity` before it drops anything an item required.

**A packet's provenance is read by a test, not only written by one.** `m3c` shipped a review round in which the compiled packet's `provenance.compiler` said `cbr-context-script`: the publication path passed the constant unconditionally, and the job's own `compiler` field was never read. Twenty-one mutants had passed over it because **no test read the field** — the mutant that swapped the two constants was caught only by the coverage line, which is built from a different one. Both halves are now asserted: a compiled packet in J1, a scripted packet in `crates/cbr-provider/tests/context.rs`.

**One timing assertion was rewritten after it failed on CI, and it is worth saying why.** `a_consumer_that_never_reads_is_closed_within_twice_the_notice_budget` bounded the *sum* of two phases with a single slack allowance. On a loaded macOS runner it failed at 861 ms against 850: the provider had declared the stall at the room deadline and closed the session one notice budget later, exactly as CORE section 11 requires, and two independent scheduling delays had added up while the one allowance did not. The test now bounds each phase separately — declared within the budget, then closed within the budget of *being declared* — and keeps the total as the composition of those two. The same behaviour is pinned; what changed is that a scheduler delay in the first phase no longer eats the allowance for the second. The provider's unit-test binary grew from 57 tests to 68 this milestone, which is the contention that surfaced it.

**A limit M3 recorded and m4c resolved: the index build ran inside the preparation tick.** A job that needed an index built it while holding the provider's processing lock, so every other job and every request to that provider waited for it — **about 7 seconds for 1,066 blobs** on this repository, once, on the first request at a tree. It was left in place at M3 rather than moved cheaply, because moving it means a bounded runtime with its own budget and its own failure reporting, and doing it without one would have meant an unsupervised thread with neither. m4c gives it that runtime: the build goes on a bounded work pool, the tick asks where it has got to and returns, and a build that fails or outruns the request's deadline is a projection of state `unavailable` naming why rather than an empty one. Measured before and after on the same machine and the same three trees, an unrelated job's worst wait falls from 8.5s, 11.1s and 3.4s to 0.13s, 0.23s and 0.15s, while the time to a first packet is unchanged — [the measurement and what it does not establish](work/m4/STALL.md).

**Compiling is production-only.** A conformance launch is a test harness, and there a request with no script is still a request nothing prepares — which is what every context fixture was measured against. All seven suites are unchanged.

**What this does not establish.** That the packet is useful: the compiler's determinism is a property of the build, and INTERNALS section 4 is explicit that a structurally consistent build is not a claim about whether the content is right. Ranking is still BM25 and is still unevaluated. The selector chooses which span of a named file to cite; it does not choose which file to look at, because the request's items name the files. A model-assisted selection is M4.

### The `cbr` command, and the credentials it needs

**`cbr ingest` and `cbr fetch` are a client, not a shortcut.** `cbr` is a separate binary that depends only on `cbr-encoding`: it opens the provider's Unix socket, authenticates, negotiates `core/1` with `core.events` and `evidence/1`, and uses `evidence.upload.prepare`, `append`, `seal` and `evidence.fetch`. It links no provider code and opens no store. `fetch` checks the bytes it assembled against the digest it asked for, which is the sealed digest, **before** writing anything, and writes nothing on a mismatch.

Issue #3's acceptance is `ingested_bytes_fetch_identically_after_sigkill_and_restart`: 3 MiB and 123 bytes, several chunks each way, ingested over the socket; the provider is killed with `SIGKILL` and started again over the same data directory; `cbr fetch` returns byte-identical content matching the sealed digest. The same test checks that a wrong digest is `artifact_digest_mismatch` and writes nothing, the modes of the credential file (`0600`), its directory (`0700`) and the socket (`0600`), and that the credential appears in no output of `cbr` or the provider. `fetch_refuses_bytes_that_do_not_match_the_sealed_digest` restarts the provider with the `serve_altered_bytes` control, so the provider serves altered bytes behind honest metadata: `cbr` refuses them. That test exists because the provider's own integrity check would otherwise hide whether `cbr` checks at all.

**`cbr expand` follows a packet's citation back to the bytes it cites** (`context.expand`, RELEASE-SCOPE §2's "expand a citation"). It reads the request's last published revision unless `--revision` names another, found as `cbr packet` finds it, and asks from `--offset` for what is still missing until `--length` bytes are here or the artifact ends. The provider may answer shorter than asked — it halves a read until it fits the caller's 1 MiB frame — so the client keeps asking from where the last answer ended; an offset past the end, an empty answer before the end, more bytes than asked for, an answer from another offset, a size that moves and a citation whose evidence changes between reads are all errors, never trimmed. A whole artifact is checked against the cited digest before anything is written or printed, as `fetch` does. With `--out` the bytes go through a staged file and one line says what they are; without it they are the whole of standard output. `crates/cbr-cli/tests/expand.rs` runs it through `cbr` against the real provider: the whole artifact is the ingested file and what `cbr fetch` returns; every range tried is the source at exactly that range, on standard output and in the file; an offset past the end fails and writes nothing, and a zero `--length` is refused in the client's own words with no provider listening at all; **an unknown citation, one whose artifact the grant cannot read and one in a request the grant cannot read fail with the same words and no file**, and with no `--revision` a request the grant cannot read fails exactly as one never submitted; `--revision` picks that revision's citations; a 1.2 MB artifact, larger than one frame, comes back whole; under a launch whose `provider_id` is not `cbr`, a compiled packet's citation names that id and expands, for an evidence item and, since `cbr-context-compiler/4`, for a source item, whose citation named `cbr` literally until then; **every citation of a compiled packet over a hostile repository** — a 300-byte path, spaces and letters outside ASCII, a `.hidden/` directory, and the same path in two repositories — expands to its `git cat-file` bytes, whole and at the range its locator names; and J1 expands every citation its packet gives; and **a scripted citation naming another provider fails with the same words and no file as an unknown one**, even though this store holds an artifact of that id and digest, while the same artifact cited under this provider's id or none expands. `cbr context` writes an evidence item with no provider, which is the provider being asked, and **compiling cites it under the provider's own id and refuses an item naming another provider as `evidence_unavailable`**, even when this store holds an artifact of the same id and digest — `a_compiled_evidence_item_is_read_only_at_the_provider_its_reference_names`, in `crates/cbr-provider/tests/context.rs`. The read loop, each answer, the digest check and the delivery are also unit-tested in `context.rs`: a provider that answers short, empty, over-long and from elsewhere; evidence that changes between two reads and an excerpt whose length is not its bytes, both refused; and a mismatched digest that leaves no file, no staged file and nothing on standard output, which is `deliver`, the function `expand` hands its bytes to.

**What `cbr expand` does not establish.** That a second revision arises from a compiled request: a compiled request publishes once, so the revision test is scripted. That the digest check fires end to end: the provider refuses to serve an object that does not match its digest, and no control makes it serve altered bytes to `context.expand`, so the client's check is held by the unit tests of `deliver` alone. That a write interrupted part-way leaves nothing at `--out`: writing in place instead of through the staged file survives mutation, as it does for `fetch`. That a very large artifact is cheap to expand: every `context.expand` call makes the provider read and re-hash the whole object, so expanding all of one costs about size²/512 KiB of provider work — the largest artifact here is 1.2 MB, read in two answers. And `--offset` and `--length` are global options, so other verbs accept and ignore them, as they already do the options of other verbs.

**Credentials (CORE §18.1).** A production socket start issues `ccred1.<principal>.<secret>` from `/dev/urandom` if no live handed-off credential exists, stores only its SHA-256 with its principal and revocation state, and writes it to `credentials/<principal>` in the data directory. `cbr-provider --rotate-credential` and `--revoke-credential PRINCIPAL` are administration commands that run beside a serving provider; `core.authenticate` reads the store each time, so both apply to the next authentication. `a_rotated_or_revoked_credential_no_longer_authenticates` shows the rotated-out and revoked credentials refused, and a restart reissuing after revocation.

**What `cbr` does not do.** An ingest interrupted part-way is not resumed by a later `cbr ingest`, which creates a new artifact; the partial one stays staged, is never served, and is not timed out, because staging timeouts exist only as a test control. The credential file protects against other operating-system users, not against programs running as the owner (CORE §18.3).

### The model runtime

**No model has been called by anything in this suite.** Everything below is established against a fake transport and against hand-written fixtures, and that is the ceiling on what the suite can establish. Models have been called outside it in eight live sessions, each on the owner's word at the time or under the owner's standing grant of 2026-09-25: two calibration runs, by hand, recorded in [CALIBRATION](work/m4/CALIBRATION.md) and [READINESS §7](work/m4/READINESS.md#7-what-may-be-sent); and three runs of the m4e harness — 65,144 tokens, then 8,473 in a run that stopped at its first replay, then 67,395 — recorded in [JOURNEYS](verification/JOURNEYS.md#live-run-3-2026-09-23-both-pilots-and-j1-again-after-m4f-and-m4g); J2's two invocations of 2026-09-25, 36,410 and 83,657 tokens, recorded in [JOURNEYS](verification/JOURNEYS.md#j2-live-2026-09-25-the-mechanism-held-and-the-model-made-the-projection-worse--failed); and the rerun's second invocation of 2026-09-26, 46,452 tokens — its first asked nothing and called no model — recorded in [JOURNEYS](verification/JOURNEYS.md#j2-live-rerun-2026-09-26-passed--and-the-model-added-nothing). Where a live run has since shown something the suite cannot, the row below says so.

| What the tests establish | What they do not |
|---|---|
| The **envelope**: a local byte bound that never falls below what any byte-level tokenizer could emit, two counters checked independently, per-request, per-job and per-run ceilings, a durable ledger whose reservation is written before the send, a completion that settles to the provider's own figure rather than to the reservation that over-stated it, two admissions racing for the last of a window admitting exactly one, and six crash-matrix rows — now killing a provider in the middle of preparing a **real request**, after each of which the spend is counted at least once and never zero. **Since m5-arith, what one question can hold**: `model::question_worst`, its first send and its widest repair, each send priced as admission reserves it (`model::send_worst`, `budget::reservation`), and each published figure the most over every dialect a launch can configure (`model::over_every_dialect`, over `wire::Dialect::ALL`, the one list `Dialect::parse` reads a configured name from; a test holds every configurable name to it, and the admission test runs every dialect). A sweep over a grid of ceilings on the real call path finds no serving question holding more, on two synthetic bodies and on every published widest body; **since m5-settle**, a count reserves the completion's input bound and settles within it, and a completion admitted on a count is refused when the two would pass the send the local bound refused, so with room freed at every place in that window the call path names no question holds more than `question_worst` either (896 and 952 runs, the most 2,812 and 14,786, each equal to it), and the published bodies' maxima are unchanged (129,018, 388,474, 167,307 and 84,106); a source test holds both serving launches to `model::SERVING_COUNTING`; and a test shows each remaining route — a completion, a failed completion or a failed count billed above its reservation — charged whole, recorded as an `overrun` naming its job and request, ending its call `reservation_overrun`, and every later admission refused, for another job and after a restart, because the stop is read inside `Ledger::admit` before every ceiling, as `bound_unsound` now is ([m4 READINESS §3](work/m4/READINESS.md#what-model-assisted-discovery-costs-against-the-three-ceilings)) | That the bound holds against the provider's own tokenizer. **No count of CBR's has been compared with MiniMax's**; that is the calibration ([READINESS §10](work/m4/READINESS.md#10-the-calibration-and-what-stops-m4)), and one provider count above its local estimate stops M4. **Nor that a bill stays within its reservation**: the published figures bound what admission can reserve. A bill above a reservation is caught and stops the store; it is not prevented, and a store can pass a ceiling once, by what the calls in flight when it settles bill above their reservations ([ADR 001](decisions/001-standalone-v0.1-scope-and-stack.md) question 18). Shown only against the fake. |
| The **credential path**: an absolute tool path, arguments fixed at compile time, a cleared environment, null standard input, discarded standard error, a timeout, and a refused launch for every failure with no fallback to any other source on any platform | Anything about the real Keychain. **Every test runs against an injected fake tool**, so no test on any machine reads the owner's key. The Linux refusal is established only by CI's Linux job. |
| The **wire**: both dialects' serializer and parser, the generation limit bound to the member each dialect's provider reads, `reasoning_split` set, whole responses only, a `<think>` marker refused, and prose-where-a-structure-was-asked and text-where-a-tool-was-demanded handled as ordinary outcomes with one bounded repair charged to the same ledger | That the provider sends what these fixtures say. **Every fixture is hand-written from public documentation and unverified against the live service**; the count response's shape and two exhaustion status codes are explicitly guesses that degrade to a refusal rather than to a wrong answer. |
| **Redaction at the recording boundary**: the store accepts only bytes that have been through it — a type-level property, so moving redaction after the write is a compile error — and a scan test opens a real store file and reads back every byte of every file it leaves behind, looking for a planted key and signature in a presigned URL's query string | That every credential shape is recognised. A credential is not recognisable by looking at it; the rules key on parameter and member names, scheme words and vendor prefixes, and a negative control asserts ordinary text is untouched. |
| **The calibration** ([READINESS §10](work/m4/READINESS.md#10-the-calibration-and-what-stops-m4)) runs end to end against the fake transport, including its stop condition: a provider count above its local estimate halts the run, says so, and sends nothing further | Anything about the live service — which the suite cannot show. **It has been run twice**, on the owner's word: run 1 stopped at its first call, and run 2 measured the bound, which held on every file ([CALIBRATION](work/m4/CALIBRATION.md)). |
| **The serving call site** (m4c): a request that authorised an investigation asks a model which of the ranked spans to cite, on a bounded work pool, with the request's deadline. The answer is one of a **closed set of ids CBR offered**, so an id it never offered, a structure that is not a choice, a provider failure, a refusal at admission and a timeout all end as the item's typed unmet reason rather than as the span BM25 would have chosen. A request that authorised none calls nothing at all, and nothing outside its view is in a request body. **Since m5-arith a candidate is shown within 8,192 bytes of text and 1,024 of path as the body carries them**, through one frame every step shares (`selection::shown`), and cut with the marker `[cut]` past either, never inside an escape; a test holds text that escapes to at most two bytes a character byte-identical to the frame before the cut | Whether asking improves a packet. The model chooses between spans BM25 already ranked, and nothing here says the choice is better. **That is m4e's journeys**, scored against the deterministic runs as baselines. Nor whether a model chooses as well from a cut candidate as from a whole one: no live run has shown a model one ([ADR 001](decisions/001-standalone-v0.1-scope-and-stack.md), question 17, accepted). |
| **Derivation records and replay** (m4d): every exchange a request takes an answer from is sealed as an evidence artifact — model, admission path, usage, latency, the candidate set offered and the id chosen — **holding no repository text**; a call whose answer is nobody's, because the request was cancelled, seals nothing while its spend stays in the ledger; the record is served only to a reader whose view covers it, at all three doors — `fetch`, `inspect` and a listing, and the m3c byte-identity assertion says a reader outside the job's view gets the packet they would have got in a store where no model was called. `--replay-model` rebuilds a packet offline from the retained records, with a transport that panics if reached, and the digest matches the one the call produced. **A rebuild is a function of the records rather than of their order**: every covered record for a question is read, they must agree, and a purged one is not read at all. **A record's usage is its question's whole charge** (m4h, format `/3`): every attempt, as the ledger settled it, equal to the question's ledger rows; a `/2` record still replays | That a record is enough to *explain* a choice. It says what was offered and what was picked, not why. And nothing here ages a record out: [READINESS §6](work/m4/READINESS.md#what-is-retained-and-for-how-long) states what is retained and for how long, and **the policy is M6's**. |
| **Model-assisted discovery** (m4e): a model may propose **search terms**, bounded by count, by length and by characters, and CBR runs them through its own index inside the view — the same tokeniser the request's own words go through, so a term shaped like a path is plain words and there is no branch that resolves one. The union is a closed set it chooses ids from, with the labels, ranks and citations the deterministic path gave them; the ordinary reading takes a reserved share of the set so a term always has room to add something. Negative control 5 is run against both steps with the planted file really in the candidate set: a path proposed as a term resolves to nothing, an id never offered selects nothing, prose fails the character bound, and each is a typed unmet, a recorded derivation and nothing widened. A failed step leaves the deterministic reading and the packet declares it `unavailable`. A **binding** claim is carried whether the model listed it or not. Both steps show candidates under m5-arith's cut, and **an escape-dense repository is asked its choice within the published flow** (`model_discovery.rs`): at `651c86e` that fixture's choice was refused at 402,821 tokens, over the per-request ceiling, and it is now asked at 142,541 of the 193,548 a choice can reserve | Whether a term the model proposes is a *good* term. It still has to occur in the repository, so this makes CBR look where its own reading did not suggest and does not make it find what is not written down. **And nothing here is evidence that a packet improved**: that is the live run, scored by the reviewer against the deterministic baselines. |
| **The live-run harness** ([`scripts/m4e_run.py`](../scripts/m4e_run.py)): its dry-run mode drives submit, settle, packet, ledger and the replay gate against the fake transport, and its refusals — a checkout whose **origin** is not one of the three public repositories (the id is a label a manifest typed; the origin is read from the checkout), a checkout off a commit the manifest pins, **in live mode an origin `api.github.com` does not say is public** (unauthenticated, one call per distinct origin, only a 200 with `"private": false` admitting one — a dry run makes no such call and no test in the suite does either, so only its parser is tested, on canned bodies), an investigation budget the items would exhaust before discovery was reached, a model outside the three M4 names, an output directory inside this repository, `--live` without `--permit-model-network`, a ceiling above 5,000,000 or a stop above 2,250,000 — are made before a provider is launched. Each launch is given the run ceiling **less what the launches before it spent**, because the ledger sums one store and every run opens a new one; the stop is checked before a run against that run's computed worst case — since m5-arith its flow and one selection question per want, 518,628 + wants × 167,753, where before it priced the flow alone — rather than after one that had already crossed it, and the report states both figures and each run's price, and a stop's message the whole figure and the wants that make it, by test; since m5-settle the harness sums only the ledger's own spend kinds, held to `Ledger::SPENT` by test, and starts no further run after one whose store recorded `bound_unsound`, `count_unsound` or `overrun`, and `j2_run.py` does the same, by test; every work directory is under `--out`, which is where the ledger and the records the gate replays live, and since m4h **the harness reads a store without writing a byte of it** — with a log and a shared-memory file `mode=ro` with a read-only shared memory, with no log `immutable=1`, with a log and no shared-memory file from a private copy — where before a read-write connection could checkpoint a store it read; and every run's report counts the `discovery.*` records that were sealed, because a run that never reached discovery produces a packet that reads like one the model did not widen. The **replay gate** reports every question whose retained records disagree, with both records and the answers, and — when the rebuilt packet differs — **every leaf at which it differs**, carrying a value only when it cannot be repository text. The rebuild is launched under the same configuration the live run used, because a conformance one names a different provider in every citation and the first live run's six reported differences were all that and nothing else. **The credential each launch presents is read off that configuration**, so the two cannot be decided in two places — they were, and the second live run stopped on it at `authentication_failed`; only a live run exercises the production side of that rule, and the suite asserts the decision rather than the launch | That a live run works — which the suite cannot show. **It has been run three times**, each on the owner's word at the time: run 1 completed, run 2 stopped at its first replay on the credential defect described to the left, and run 3 completed with every replay identical and every rebuild authenticating ([JOURNEYS](verification/JOURNEYS.md#live-run-3-2026-09-23-both-pilots-and-j1-again-after-m4f-and-m4g)). The gate compares the rebuilt packet's *sections*, because a sealed packet names its own request and two request ids can never be byte-identical; byte identity across two stores is `derivation_replay.rs`, not this. |
| **Journey 2's projection** (m5a, `project_large_result`): an `evidence_included` item's section is a projection of the sealed artifact — its run identity (the capture anchors it was sealed with, each shown escaped on one line, and the run's own lines while they fit), the failures it names, each as its bytes at a stated range, and a ledger that **tiles the artifact**: excerpts that are its bytes at their stated ranges, and omissions declared with a reason and, under the protocol's own reason, in the packet's omission list. Cargo's test output, a JSON document and JSON lines are read by their structure and other text by its lines; bytes that are not text are declared omitted whole. **The rule's projection is a floor the model adds to** (m5a-3, `cbr-project-large-result/2`; in m5a a model's choice replaced the rule's, and J2's live run scored the result below the rule on a failing log). With investigation, a model is asked what to add: it is offered, as a closed set of ids per part, only the units the floor leaves out that could be added to it, drawn at the model arm's widest label; a part holds at most 32,768 bytes of candidates **as the request body carries them** and 64 ids, the questions offered are claimed together against the investigation limit, and each is its own sealed record. A question no answer could change — a floor with no room beside the model's label, or nothing it leaves out that would fit — is not asked, and the header says no parts. More than 4 planned parts, or an artifact over 131,072 bytes, is `insufficient_capacity` with no section and no call, whatever a model would have been offered, **decided before anything is offered**, and the part bound is asserted at the call site with an input under the size bound. **Where nobody may be asked** — no model, or no investigation — nothing is offered and no projection is drawn to plan; the header's part count is worked out from the floor the rule's rendering draws anyway, so both arms print the same count, and **the floor is drawn once a request** (a test-only count of drawings holds each). The model arm carries every byte the rule carries, at the same ranges, whatever the model answers, and a failure the parser found is never `not_selected`; an excerpt is wholly the rule's or the model's, and the header names the model's by number; a model arm that would not fit is refused, never published. Each question's preamble says, in counts, what is carried and the room left, as the rule's section implies. The header counts failing tests and cargo errors apart, a compiler error in cargo's JSON among the errors, a cargo error stops at the next run's identity, and an empty list at a document's root is identity. **Both of J2's negative controls** are asserted end to end through `cbr` against `model.fake` (`journey_two.rs`); every bound is reached by a fixture, and the worst part's send (41,628) and a whole projection's serving worst case (341,216) are computed from real request bodies (`projection::tests`), which also holds `j2_run.py`'s stop, 492,204, at or above it. The artifact's readability is resolved at the command: one the submitter may not read is `evidence_unavailable`, exactly as one never sealed is, and so is one purged or one whose stored bytes no longer match its digest, and a part's record is readable only by a reader who may read the artifact it showed. `scripts/j2_run.py` is J2's live harness; `j2_harness.rs` drives its dry run and its refusals, first among them **an input that carries a machine path**, and a `--run-ceiling` above m4e's hard cap of 5,000,000; its checks name a model-assisted projection that drops a byte the baseline carries, and a run whose baseline needs no question still makes the assisted request and spends nothing | That a projection is **useful**: nothing here measures whether the excerpts chosen are the decisive ones. That is J2's live run on CBR's own test output ([m5 READINESS §10](work/m5/READINESS.md)), **which ran on 2026-09-25 at `/1` and failed** ([JOURNEYS](verification/JOURNEYS.md#j2-live-2026-09-25-the-mechanism-held-and-the-model-made-the-projection-worse--failed)): on a failing test log a real model's choice carried less than the rule's, which no test here could show, because the fake answers every part alike. At `/2` it was rerun on 2026-09-26 and passed ([JOURNEYS](verification/JOURNEYS.md#j2-live-rerun-2026-09-26-passed--and-the-model-added-nothing)): no run worse than the rule and every run equal to it, because on the one input where a model was asked it added nothing, so usefulness beyond the rule is still unmeasured. That the parsers recognise every shape cargo or the conformance runner can print: they read the shapes this suite writes, and anything else is read as lines, which is safe and less useful. |
| **No socket is opened**: the transport cannot be constructed without a permit that only `--permit-model-network` produces, one test asserts the gate has exactly one caller and that it is the launch, and two more assert that four of the five crates reach no network client at all while the fifth names in code exactly what it added | That a process given the flag behaves correctly against a real endpoint. Nothing here has run against one. |

**The survey behind the candidate cut is a command**, run from the repository root at any commit (added at m5-arith):

```sh
python3 scripts/escape_scan.py 651c86e
python3 scripts/escape_scan.py --json 651c86e
```

It reads that commit's tree through `git rev-parse`, `git ls-tree` and `git cat-file` alone, rebuilds the spans retrieval can offer — the indexer's regular files up to 1 MiB and valid UTF-8, its chunks, each clipped at retrieval's 4,096 raw bytes and decoded as the provider decodes it — and counts what a request body carries of them: the widest span, how many pass 4,096 and the cut of 8,192, how many hold a six-byte escape, the longest path and how many pass 1,024, and every span and path a model would be shown cut, by path and lines. It prints the commit, counts and repository-relative paths, each path escaped as a body carries it and DEL and the C1 controls escaped too, so no control character of a path is printed raw, **and never repository text**, and it writes nothing. `crates/cbr-cli/tests/escape_scan.rs` holds it to a fixture with one of each shape, including a chunk closed at exactly 2,048 bytes, a clip inside a character, a blob of exactly 1 MiB and a path at exactly 1,024 carried bytes, and to a path of C0, DEL and C1 controls printed escaped; `selection::tests` holds its six bounds to the code's. Its results at the commits the live runs used are in [m4 READINESS §5](work/m4/READINESS.md#what-a-candidate-shows-a-model-and-where-it-is-cut).

**What it does not establish.** That any span it counts was shown to a model: retrieval ranks spans and a question is shown a handful, so the scan bounds what could be cut rather than recording what was. Anything about a dirty working tree, since it reads a commit. And anything about the provider's tokenizer, since it counts bytes.


#### Four rules that keep the owner's key out of every run

**These are rules, not conveniences.** A reviewer or a future test that launches a valid model configuration on macOS reads the owner's real Keychain entry, and no test in this suite has any business doing that.

1. **Configuration is validated before the credential is read.** Provider, dialect, model and the absence of any endpoint member are all settled during `Config::load`; the credential read happens afterwards, in `run`. So a launch that was never going to work is refused identically on every machine, and the Keychain is never touched to discover it. The tests for those refusals are therefore safe to run anywhere, which is why they are not platform-gated.

2. **A configured model requires `--permit-model-network`, and that check comes before the credential read too.** This is what makes the first rule enforceable rather than a convention: a test cannot reach the Keychain by configuring a model, because configuring one without the flag is a refused launch. **Exactly one test passes that flag**, `a_configured_model_with_no_keychain_is_a_refused_launch`, and it is gated to platforms with no Keychain — which is the only place its claim is true and the only place it is safe.

3. **The launch decision is a pure function, and every row of it is tested without starting a process.** `launch::decide` takes the arguments and the configuration and returns one of four decisions; whether a credential is read is a property of the decision, and `launch::tests` states it over *every* combination of inputs rather than over a table somebody remembered to extend. Before that, rules 1 and 2 rested on **the order of statements in `main`**, checked only by process-level tests asserting that the message did not mention a keychain — an assertion that would not have noticed a successful, silent read, which is the failure that matters on the owner's own machine.

   The rule had teeth because of what it revealed: while `launch::SERVING_CALLS_A_MODEL` was false, **a serving launch with a model configured was refused**, because the build had no call site to spend a credential at and [READINESS §2](work/m4/READINESS.md) forbids reading one into a process with no use for it. So the calibration was the only launch that read a credential at all. **m4c set that constant true**, in the commit that gave serving a call site, and the tests constraining that row were written as implications so they stopped constraining it rather than having to be found and deleted.

   **What the flip costs, and what pays for it.** `--permit-model-network` with a valid model and no `--calibrate` is now a launch that reads the owner's Keychain and serves, where it used to be a refusal — which is the exact shape of the lapse this rule exists for. So the rule moved out of the constant and into a test that reads the sources: `tests/credential_discipline.rs` requires every place a test passes that flag to be gated off macOS, to be asking for the calibration, or to configure no model at all. It reads text, so a program that builds the flag out of pieces would slip past it; it is a guard against carelessness, which is what the lapse was, and not against intent.

4. **On the owner's machine, the provider is launched only through the test suite, an authorised run, or `scripts/debug_launch.sh`.** Not by hand, not to check a refusal, not to see what a flag does. The three rules above are about what the *code* permits; this one is about what a person does with it, and it exists because the code's permission was found the hard way: the credential read that [STATE](work/STATE.md) records came from improvising a launch at a shell prompt to probe a refusal. Rule 3 now makes that particular launch impossible, which is the right kind of fix — and the habit is still the rule, because the next gap will be somewhere rule 3 does not reach. A refusal worth checking is worth a test.

   **The rule was then broken a second time**, while debugging m4c, and a rule broken twice is a rule that is not working. So m4d stops restating it and makes the permitted path the easy one: `scripts/debug_launch.sh` starts a provider a person can poke at — a throwaway data directory, a socket, and the credential file `cbr` wants, all printed in the form it wants them — and **cannot be talked into calling a model**. It writes its own configuration, in the production format, naming no `model_runtime`; it refuses `--config` so it cannot be handed one that does; and it refuses `--permit-model-network` and `--calibrate` by name, before anything starts. `launch::decide` then returns `Serve`, which reads no credential, and nothing in the process can open a socket to a provider because no permit exists.

   **This is a hand launch and it is the only one.** Anything it cannot do — a live call, a refusal that needs a model configured — is a test, or is an authorised run: on the owner's word, or since 2026-09-25 under the owner's standing grant ([m5 READINESS §10](work/m5/READINESS.md#10-live-calls-each-planned-estimated-and-capped-before-it)), planned before it. `crates/cbr-cli/tests/debug_launch.rs` holds the script to its claims: the two flags and `--config` refused with nothing started, the configuration it actually execs with parsed and asserted to name no model, and the launch served end to end through `cbr` over the socket it printed. The argument vector is read from a stub the script execs, so *passes no model* is checked at the exec rather than by reading the script for reassuring text.

A change that moves either check later, or that adds a second ungated test passing `--permit-model-network` with a valid model, breaks these rules — and the second of those is now a failing test rather than a convention. Anything needing a live launch on macOS is an authorised run, planned before it, and never the suite's.

### The storage crash matrix

[STORAGE §5](https://github.com/Combraton/combraton/blob/main/docs/architecture/STORAGE.md) names the crash boundaries. **Each row that exists in CBR at M1 is fault-injected by killing the real provider at that line.** A test barrier inside the provider (decision 007) creates `<name>.reached` and waits; the test sees the marker, sends `SIGKILL`, confirms no response frame was written, restarts over the same data directory and asserts the durable fact through the protocol. The tests are in `crates/cbr-provider/tests/crash_matrix.rs`; **the six model rows moved in m4c** to `crates/cbr-cli/tests/model_crash_matrix.rs`, where a client can submit the request whose preparation they kill.

| Row | Killed at | Durable fact asserted after restart |
|---|---|---|
| Before command commit | `evidence.append.before_commit` | Nothing accepted: `received` and revision unchanged, no event; the same command ID then applies once, not as a replay |
| After accepted, before the caller received the acknowledgment | `evidence.append.after_commit` — the `SIGKILL` between append and acknowledgment | The append is durable; the same command ID replays with the original revision; `received` is not doubled; exactly one `appended` event; the sealed content is byte-identical |
| During artifact upload | idle between appends, then `evidence.append.before_commit` on the next | Still staged with the durable `received`; `evidence.fetch` is `not_found`; seal is `upload_incomplete`; no object, no `sealed` event; the upload resumes from `received` and fetches byte-identical |
| The orphan object (STORAGE §2) | `evidence.seal.after_object_published` — object published and verified from disk, row not committed | Before restart the object is on disk, complete. After: collected; the artifact is still staged at its old revision, `fetch` is `not_found`, no `sealed` event — **no receipt for the seal**. The same seal then applies and publishes again |
| During collection | `evidence.purge.after_commit` — row committed, object not deleted | The artifact is `purged` and fetch serves nothing; the deletion was finished at start; the purge replays. When another sealed artifact shares the bytes, **the object is kept** and still served for that one |
| A context packet's object published, its batch not committed (m3a) | `context.packet.after_object_published` — the packet's object published and verified, its request, job and artifact rows not committed | Nothing was reported before the kill, and at the barrier the object is on disk, the request still `preparing` and no artifact row names it. After restart exactly one revision is published, with one `sealed` and one `packet.published` event, and `evidence.fetch` serves bytes matching its digest. This row's test is in `tests/context.rs` |
| A refused packet's job ending built, not committed | `context.packet.refused_before_commit` — the publication guard refused the packet, the tick's batch was dropped, and the batch refusing the request and ending the job is built and not committed | Nothing was reported or logged before the kill, the request is still `preparing` and the job `running` in the store. After restart the request is `refused` as `packet_invalid`, with exactly one `context.request.changed` to `refused`, one `context.job.ended` and one log line. The same barrier, with SQLite's write lock held from a second connection while it waits, makes that commit fail instead: the tick fails, nothing is logged as refused, and the next tick ends the job once. This row's two tests are in `tests/context.rs` |
| A context packet sealed at an evidence peer, its batch not committed | `context.packet.after_peer_sealed` — every packet of the tick sent to the evidence peer and sealed there, its capture instant committed before the first send, the tick's batch not committed | At the kill the peer holds the packet sealed at the first attempt's `captured_at`, and the request is still `preparing`. After a restart with the clock moved, the revision is published once, under the peer's digest and that `captured_at`; so is each of two packets one tick sealed. The same barrier, with SQLite's write lock held from a second connection, makes the commit fail instead, and the next tick, the clock moved, publishes it. With the lock held from before the tick, the instants cannot be kept and nothing reaches the peer. This row's tests are in `tests/context.rs` |

**Rows that do not exist in CBR at M1**, and so have nothing to kill: *after commit, before dispatch* — CBR dispatches no effects until M4's model calls; *after harness prompt write*, *after invocation dispatch right* and *after completion commit* — PIO's; *after seal, before adoption* and *after adoption, before the provider sees the decision* — Comreton adoption, which standalone CBR does not have; *during branch creation* — Knowledge, M2; *while CBR projections lag* — no projections exist before M2.

**Recovery is a start-time collection pass.** When a provider opens its store, before any session exists, it rechecks the roots — every sealed artifact whose purge, if any, is unconfirmed — and deletes every object no root names, and every staging file an interrupted publication left. Both crash orders are deliberate: a seal publishes before its row, so a crash leaves an unnamed object and never a row naming missing bytes; a purge commits before deleting, so a crash leaves a tombstone over bytes the pass then removes. **No reader can observe `purged` while those bytes remain**, unless deletion itself fails (below): in a running provider the deletion happens under the processing lock before the response or any event delivery, and after a crash the pass runs before anything is served. Because that pass deletes what no committed row names, **a second provider over the same data directory refuses to start**; the lock is a `flock` the kernel releases when the process dies.

Every row's test fails under at least one mutant, listed in STATE, including reversing each of the two orders, removing the pass, and writing `received` outside the command's transaction. **The during-upload row is failed only by a probe** that moves a barrier across the commit rather than breaking product code: it shows the test can tell which side of the commit the kill landed on, not that it catches a particular defect.

**What the crash matrix does not show.**

- **`SIGKILL` is process death, not power loss.** Writes the process completed survive in the operating system's cache, so these tests cannot tell `synchronous=FULL` from `OFF`, or an object file that was `fsync`ed from one that was not. Durability against power loss rests on the PRAGMA read back from the open connection and on `fsync` of each object and its directory, not on a test.
- The kills are made over stdio. The barriers sit in the command path both bindings share; the socket's own `SIGKILL` test is stage (d)'s.
- An error rather than a crash is recovered only at the next start: an object whose seal commit returned an error stays until then, and so do the bytes of a purge whose deletion returned an I/O error after its row committed. That second case is the one way a reader can see `purged` while the bytes are still on disk. Nothing serves them.
- Only evidence artifacts own objects. A context packet sealed in CBR's own store is an evidence artifact, so it is a root like any other. Anything that later publishes into the object store outside an artifact must become a root before it does, or the pass will delete it.

### The socket binding, and what it cannot show here

**The different-user branch of the peer check is a coverage limit on this machine, not a pass.** STREAM §6 requires a connection from another operating-system user to be closed without a frame. The socket directory is `0700`, so an ordinary second user cannot reach the socket at all; the only way to reach the provider's own peer check is a user that bypasses directory permissions. Protocol's CI does that by connecting as **root through passwordless `sudo`**, and root is the only other user it tests. This machine has no passwordless `sudo` (`sudo -n true` asks for a password), so CBR exercises only the positive control: a unit test that a same-user connection's peer is this user. The refusal is implemented with `getpeereid` on macOS and `SO_PEERCRED` on Linux, and has no test that could fail if it were removed.

**Two socket fixtures passed vacuously before the binding existed.** `socket.malformed-clock-file-refused` and `socket.unsafe-directory-refused` require only that the provider fails to start, and an unimplemented binary failed on the unknown `--socket` flag. They are now backed by mutants: removing either check makes its fixture fail at step 0 with "participant listened".

**Committed socket and composition transcripts contain the runner's synthetic test credentials.** The conformance README says they will: the runner derives a deterministic credential per principal from the run's temporary directory and authenticates each session with it. They are not real credentials and grant nothing outside that run. The redaction applied to machine paths does not apply to them.

### Two features with no fixture evidence

**`core.effects` and `core.events.backpressure` have no fixture evidence in CBR's role.** At the pin, 70 fixtures declare one or both features, across `core`, `socket`, `execution`, `composition`, `compat` and `verification`, and **every one of the 70 also declares the `execution` profile**, which CBR never serves, so none can run against CBR. One further fixture, `core.negotiation.execution-requests-against-any-provider`, mentions `core.effects` inside a negotiation request without declaring it; it runs and passes, and exercises nothing about effects. The `execution` fixtures that exercise effect semantics through execution operations — for example `execution.unknown-effect-outcome-is-not-not-found`, `execution.aborting-an-obligation-leaves-the-effect-unknown` and `execution.effects-resolve-only-with-target-authority` — informed CBR's tests, which is not the same as passing them. Both features are implemented and declared, and **both rest entirely on CBR's own tests**, each mutation-checked:

- **`core.events.backpressure`** — `session::tests` drive the real session loop and provider against a writer the test holds or delays. They show a consumer that never reads is declared too slow at the room deadline and closed within twice the notice budget, with output before closure measured within the bound and three subscriptions sharing one notice budget; a session that did not negotiate the feature is closed without a notice; a consumer that returns within the budget receives `consumer_too_slow`; and withheld notifications are delivered later, in order, with none skipped. One integration test shows the same over real pipes: an unread standard output ends the process, and the ending is recorded on standard error with the bound. **The session thread does wait on a stalled consumer — for the room deadline and no longer, and never inside a write**; that wait is the backpressure. A command's commit never waits on output.
- **`core.effects`** — unit tests on the record and provider tests through `handle`: an effect recorded in its command's transaction and read back whole; reading one under a grant refuses an existing and an absent effect identically; an obligation past its deadline is marked overdue once, by a provider-origin event, and is not satisfied by the wait ending; and aborting it leaves an `unknown` effect `unknown`. **Nothing in M1 produces an effect**; CBR's first real effects are model calls in M4.

A fixture passing is evidence another implementation agrees; these tests are evidence CBR does what its authors read the specification to require. They are not the same kind of evidence, and neither feature should be described as conformance-tested.

**Declared features are not the whole dependency.** The c3 expectation was computed from each fixture's declared features and confirmed by a measured run, and it was still one fixture short of the truth: `core.events.visibility-follows-direct-read-authority` declares only `core.events` and `core.grants`, but restarts with a changed capability and expects the provider-origin `core.capabilities.changed` event that the change records. Event recording is not gated on negotiation (CORE §16.3), so the fixture is right and the model was incomplete. CBR therefore records the capability snapshot and its change event now; the `core.capabilities` query and the `capability_unavailable` refusal remain c4, and the feature is not claimed. A later expectation computed the same way should be treated as a lower bound on what a stage needs, not an exact one. The c4 expectation was checked for the same effect and none was found: all 130 passed with only `core.capabilities` implemented.

**One test control is implemented and declared:** `clock.file`. The provider reads protocol-visible time — grant expiry and `recorded_at` — only from its clock. Under `clock.file` it follows forward writes, ignores backward and malformed ones so an expired grant stays expired, and refuses to start if the file is malformed at launch, when there is no last good instant to keep.

`core-test/1` is served **only** under a `combraton-conformance-config/1` launch configuration. A provider launched in production serves no `core-test/1`, answers `method_not_found` to its operations, and **refuses** a production configuration naming any test control rather than ignoring it. Each of those is a test, and each has a mutant that the test kills.

Committed results live under `conformance/results/`. The `manifest.json` in each is the runner's own output, unedited. Beside it, `cbr-run.json` records the runner's identity — the release archive SHA-256, the source commit and the release `Cargo.lock` SHA-256 — so a fixture outcome names the exact runner that produced it, along with a correction for the runner's `suite.protocol_commit`, which records the Git checkout enclosing `--repo` and therefore names CBR rather than Protocol. **Transcripts from `m1c3` onward have one declared substitution:** the runner expands `{repo}` in the launch argv into the checkout's absolute path, which names the machine and its owner in a public repository, so `run_fixtures.py` replaces that prefix with `{cbr_checkout}`, records the substitution and the file count in `cbr-run.json`, and refuses to finish if any other machine path remains. The earlier `m1b` and `m1c2` transcripts predate this and were **sanitised in place after the fact**, with no history rewrite: the same substitution, a note in each `cbr-run.json` saying when and where the original bytes remain in git history, and no change to any manifest or outcome. `scripts/result_paths.py` holds the one substitution and the one check; `run_fixtures.py` applies it when recording, and CI runs it over every committed results directory.

When reporting a result, give the command, its exit status, the environment and the tested revision. Preserve the producing command's exit status when shortening output: piping a failing build into a successful `tail` or `grep` reports success, and a shortened log is not evidence that the command passed.

## Standalone release evidence

Follow [standalone release gates](https://github.com/Combraton/combraton/blob/main/docs/STANDALONE-RELEASES.md). Core no-optional-service tests, protocol conformance, real-adapter integration, comparative outcomes and UI usability are separate evidence classes. The [benchmarks repository](https://github.com/Combraton/benchmarks) owns cross-product scenarios/results, not this service's normative contract. No runtime benchmark has been implemented or run by the documentation setup.
